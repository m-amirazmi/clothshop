import { createSlice, PayloadAction } from "@reduxjs/toolkit";

const initialState = {};

export const plpFilterSlice = createSlice({
	name: "filter",
	initialState,
	reducers: {},
});

export const {} = plpFilterSlice.actions;
export default plpFilterSlice.reducer;
